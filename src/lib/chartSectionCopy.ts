/** Shown in UI and PDF section headers for funnel charts. */
export const REACH_CHART_SECTION_TITLE = 'Cumulative Completion'

/** Subtitle under the reach chart title (same visual role as `DROP_CHART_HINT`). */
export const REACH_CHART_DESCRIPTION =
  'Percent of partial leads who gave every required answer on this question and all prior questions (in order). Unlike "saw" a page, the CSV only has submitted answers.'

export const DROP_CHART_SECTION_TITLE = 'Where Students Stopped'

/** Subtitle under the drop chart title, above the highlighted-bar hint. */
export const DROP_CHART_DESCRIPTION =
  'Percent of partial leads who did not advance past each question.'

export const DROP_CHART_HINT =
  'Highlighted bar = largest incremental drop between steps (same metric as the funnel table’s Incremental drop % column and the KPI above).'
